import SpriteKit

class Config {
    
    // Max depth that a branch could achieve
    static let maxDepth = 10
    
    // Branch colors
    static let branchColor: SKColor = .black
    static let accentColor: SKColor = .orange
    
    static let minBranchSize: CGFloat = 50.0
    
    static var hueOffset: CGFloat = 0.0
}
